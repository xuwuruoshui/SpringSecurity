spring:
  thymeleaf:
    enabled: true
    encoding: utf-8
    check-template-location: true
    mode: HTML5
    suffix: .html
    prefix: classpath:/templates/
    servlet:
      content-type: text/html
    cache: false
#亲测无效
#  session:
#    timeout: 60s

#超时设置
server:
  servlet:
    session:
      timeout: 60s
      cookie:
      #js脚本不能访问cookie
        http-only: true
        secure: false



